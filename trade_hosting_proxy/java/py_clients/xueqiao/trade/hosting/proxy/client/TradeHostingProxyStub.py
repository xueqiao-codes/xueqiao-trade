#
# -*- coding: utf-8 -*-
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import socket
from thrift import Thrift
from thrift.transport import TSocket
from thrift.transport import TTransport
from thrift.protocol import TCompactProtocol
from thrift.transport.TTransport import TTransportException
from comm.ttypes import *
from xueqiao.trade.hosting.ttypes import *
from xueqiao.trade.hosting.proxy.ttypes import *
from xueqiao.trade.hosting.proxy import TradeHostingProxy

PYTHON_SUPPORT_DIR='/usr/local/soldier/route_agent'
if not (PYTHON_SUPPORT_DIR in sys.path):
  sys.path.append(PYTHON_SUPPORT_DIR)
from route_finder_python import *

class TradeHostingProxyStub:
  def __init__(self):
    self.__peerAddr = None

  def setPeerAddr(self, peerAddr):
    self.__peerAddr = peerAddr

  def __getServiceAddr(self, methodName='', routeKey=0):
    if self.__peerAddr != None and self.__peerAddr !='':
      return self.__peerAddr
    return route_finder.GetRouteIp(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, methodName, routeKey)

  def login(self, routeKey, timeout, req,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('login', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingProxy.TradeHostingProxy_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingProxy.Client(protocol)
    try:
      transport.open()
      result = client.login(platformArgs,req,)
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'login', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'login', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def fakeLogin(self, routeKey, timeout, req,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('fakeLogin', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingProxy.TradeHostingProxy_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingProxy.Client(protocol)
    try:
      transport.open()
      result = client.fakeLogin(platformArgs,req,)
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'fakeLogin', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'fakeLogin', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def updateCompanyUserPassword(self, routeKey, timeout, req,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('updateCompanyUserPassword', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingProxy.TradeHostingProxy_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingProxy.Client(protocol)
    try:
      transport.open()
      result = client.updateCompanyUserPassword(platformArgs,req,)
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'updateCompanyUserPassword', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'updateCompanyUserPassword', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def checkSession(self, routeKey, timeout, session,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('checkSession', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingProxy.TradeHostingProxy_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingProxy.Client(protocol)
    try:
      transport.open()
      result = client.checkSession(platformArgs,session,)
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'checkSession', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'checkSession', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def queryUpdateInfo(self, routeKey, timeout, req,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('queryUpdateInfo', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingProxy.TradeHostingProxy_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingProxy.Client(protocol)
    try:
      transport.open()
      result = client.queryUpdateInfo(platformArgs,req,)
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'queryUpdateInfo', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingProxy.TradeHostingProxy_SERVICE_KEY, 'queryUpdateInfo', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

