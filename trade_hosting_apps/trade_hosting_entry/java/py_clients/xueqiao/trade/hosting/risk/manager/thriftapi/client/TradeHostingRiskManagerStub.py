#
# -*- coding: utf-8 -*-
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import socket
from thrift import Thrift
from thrift.transport import TSocket
from thrift.transport import TTransport
from thrift.protocol import TCompactProtocol
from thrift.transport.TTransport import TTransportException
from comm.ttypes import *
from xueqiao.trade.hosting.risk.manager.thriftapi.ttypes import *
from xueqiao.trade.hosting.risk.manager.thriftapi import TradeHostingRiskManager

PYTHON_SUPPORT_DIR='/usr/local/soldier/route_agent'
if not (PYTHON_SUPPORT_DIR in sys.path):
  sys.path.append(PYTHON_SUPPORT_DIR)
from route_finder_python import *

class TradeHostingRiskManagerStub:
  def __init__(self):
    self.__peerAddr = None

  def setPeerAddr(self, peerAddr):
    self.__peerAddr = peerAddr

  def __getServiceAddr(self, methodName='', routeKey=0):
    if self.__peerAddr != None and self.__peerAddr !='':
      return self.__peerAddr
    return route_finder.GetRouteIp(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, methodName, routeKey)

  def getAllSupportedItems(self, routeKey, timeout, ):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getAllSupportedItems', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.getAllSupportedItems(platformArgs,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getAllSupportedItems', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getAllSupportedItems', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getRiskRuleJointVersion(self, routeKey, timeout, subAccountId,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getRiskRuleJointVersion', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.getRiskRuleJointVersion(platformArgs,subAccountId,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getRiskRuleJointVersion', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getRiskRuleJointVersion', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getRiskRuleJoint(self, routeKey, timeout, subAccountId,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getRiskRuleJoint', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.getRiskRuleJoint(platformArgs,subAccountId,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getRiskRuleJoint', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getRiskRuleJoint', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def batchSetSupportedItems(self, routeKey, timeout, subAccountId,version,openedItemIds,closedItemIds,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('batchSetSupportedItems', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.batchSetSupportedItems(platformArgs,subAccountId,version,openedItemIds,closedItemIds,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetSupportedItems', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetSupportedItems', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def batchSetTradedCommodityItems(self, routeKey, timeout, subAccountId,version,enabledCommodityIds,disabledCommodityIds,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('batchSetTradedCommodityItems', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.batchSetTradedCommodityItems(platformArgs,subAccountId,version,enabledCommodityIds,disabledCommodityIds,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetTradedCommodityItems', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetTradedCommodityItems', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def batchSetGlobalRules(self, routeKey, timeout, subAccountId,version,ruleItems,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('batchSetGlobalRules', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.batchSetGlobalRules(platformArgs,subAccountId,version,ruleItems,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetGlobalRules', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetGlobalRules', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def batchSetCommodityRules(self, routeKey, timeout, subAccountId,version,rules,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('batchSetCommodityRules', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.batchSetCommodityRules(platformArgs,subAccountId,version,rules,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetCommodityRules', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'batchSetCommodityRules', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def setRiskEnabled(self, routeKey, timeout, subAccountId,version,riskEnabled,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('setRiskEnabled', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.setRiskEnabled(platformArgs,subAccountId,version,riskEnabled,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'setRiskEnabled', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'setRiskEnabled', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getRiskFrameDataInfo(self, routeKey, timeout, subAccountId,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getRiskFrameDataInfo', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingRiskManager.Client(protocol)
    try:
      transport.open()
      result = client.getRiskFrameDataInfo(platformArgs,subAccountId,)
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getRiskFrameDataInfo', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingRiskManager.TradeHostingRiskManager_SERVICE_KEY, 'getRiskFrameDataInfo', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

