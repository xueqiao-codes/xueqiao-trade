#
# -*- coding: utf-8 -*-
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import socket
from thrift import Thrift
from thrift.transport import TSocket
from thrift.transport import TTransport
from thrift.protocol import TCompactProtocol
from thrift.transport.TTransport import TTransportException
from comm.ttypes import *
from xueqiao.trade.hosting.tradeaccount.data.ttypes import *
from xueqiao.trade.hosting.tradeaccount.data import TradeHostingTradeAccountData

PYTHON_SUPPORT_DIR='/usr/local/soldier/route_agent'
if not (PYTHON_SUPPORT_DIR in sys.path):
  sys.path.append(PYTHON_SUPPORT_DIR)
from route_finder_python import *

class TradeHostingTradeAccountDataStub:
  def __init__(self):
    self.__peerAddr = None

  def setPeerAddr(self, peerAddr):
    self.__peerAddr = peerAddr

  def __getServiceAddr(self, methodName='', routeKey=0):
    if self.__peerAddr != None and self.__peerAddr !='':
      return self.__peerAddr
    return route_finder.GetRouteIp(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, methodName, routeKey)

  def clearAll(self, routeKey, timeout, ):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('clearAll', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingTradeAccountData.Client(protocol)
    try:
      transport.open()
      result = client.clearAll(platformArgs,)
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'clearAll', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'clearAll', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getNowFund(self, routeKey, timeout, tradeAccountId,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getNowFund', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingTradeAccountData.Client(protocol)
    try:
      transport.open()
      result = client.getNowFund(platformArgs,tradeAccountId,)
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getNowFund', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getNowFund', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getHisFunds(self, routeKey, timeout, tradeAccountId,fundDateBegin,fundDateEnd,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getHisFunds', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingTradeAccountData.Client(protocol)
    try:
      transport.open()
      result = client.getHisFunds(platformArgs,tradeAccountId,fundDateBegin,fundDateEnd,)
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getHisFunds', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getHisFunds', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getSettlementInfos(self, routeKey, timeout, tradeAccountId,settlementDateBegin,settlementDateEnd,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getSettlementInfos', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingTradeAccountData.Client(protocol)
    try:
      transport.open()
      result = client.getSettlementInfos(platformArgs,tradeAccountId,settlementDateBegin,settlementDateEnd,)
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getSettlementInfos', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getSettlementInfos', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getNetPositionSummaries(self, routeKey, timeout, tradeAccountId,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getNetPositionSummaries', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=TradeHostingTradeAccountData.Client(protocol)
    try:
      transport.open()
      result = client.getNetPositionSummaries(platformArgs,tradeAccountId,)
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getNetPositionSummaries', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(TradeHostingTradeAccountData.TradeHostingTradeAccountData_SERVICE_KEY, 'getNetPositionSummaries', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

